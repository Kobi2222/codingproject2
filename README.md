Clone the git repository: https://github.com/Kobi2222/codingproject2.git

Navigate to the folder where the repository is saved on your command line
run the command "npm install"

To run the storybook
npm run storybook

To run the Test
npm run test
